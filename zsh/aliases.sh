# Some systems will come with the GREP_OPTIONS value set by default.
# This sucks because newer systems have deprecated this method.
unset GREP_OPTIONS

alias grep="grep --color=auto"
alias tmux="tmux -2"
